import { FormEvent, useState } from 'react';
export function LeadForm(){const [status,setStatus]=useState('');async function handleSubmit(event:FormEvent<HTMLFormElement>){event.preventDefault();const formData=new FormData(event.currentTarget);const payload=Object.fromEntries(formData.entries());const response=await fetch('/api/leads',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)});if(response.ok){setStatus('Merci, votre demande a bien été envoyée.');event.currentTarget.reset()}else{setStatus('Une erreur est survenue. Merci de réessayer.')}}return <form className="form-card" onSubmit={handleSubmit}><h2>Parlons de votre projet</h2><label>Nom<input className="input" name="name" required /></label><label>Email<input className="input" type="email" name="email" required /></label><label>Entreprise<input className="input" name="company" /></label><label>Message<textarea className="textarea" name="message" rows={5} required /></label><button className="button button-primary" type="submit">Envoyer</button>{status ? <p>{status}</p> : null}</form>}
